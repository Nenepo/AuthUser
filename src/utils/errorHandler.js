// utils/errorHandler.js

export function handleFirebaseError(error) {
  console.error("Firebase Error:", error);
  // Log error details or send them to an error logging service
  // You can also translate Firebase error codes to user-friendly messages if needed
}


